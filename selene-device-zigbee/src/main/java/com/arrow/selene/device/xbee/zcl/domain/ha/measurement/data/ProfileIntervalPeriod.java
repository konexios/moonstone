package com.arrow.selene.device.xbee.zcl.domain.ha.measurement.data;

public enum ProfileIntervalPeriod {
	DAILY,
	EACH_60_MINUTES,
	EACH_30_MINUTES,
	EACH_15_MINUTES,
	EACH_10_MINUTES,
	EACH_7_5_MINUTES,
	EACH_5_MINUTES,
	EACH_2_5_MINUTES
}
